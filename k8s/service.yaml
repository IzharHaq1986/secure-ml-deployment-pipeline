apiVersion: v1
kind: Service
metadata:
  name: secure-ml-deployment-pipeline
  labels:
    app: secure-ml-deployment-pipeline
spec:
  selector:
    app: secure-ml-deployment-pipeline
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
