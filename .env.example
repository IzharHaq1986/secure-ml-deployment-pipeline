# Docker Compose runtime configuration for local and CI-friendly deployment.
# Copy this file to .env and adjust values only when needed.

# Port exposed on the host machine
APP_PORT=8000

# Container resource guardrails
APP_CPUS=1.00
APP_MEMORY_LIMIT=512m
